VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Direct mode: call local FastAPI (use with `npm run dev:full`). Omit to use Supabase Edge Function + VISION2VOICE_BACKEND_URL.
VITE_BACKEND_URL=http://127.0.0.1:8000
